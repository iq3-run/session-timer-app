- [AnimationController catch-up-to-1.0 swallows completion](flash_animationcontroller_forward_from_one.md) — `forward(from: 1.0)` silently never fires status listeners when the controller was already `completed`; breaks any code that computes a wall-clock catch-up `from:` value and depends on the completion callback to advance state. Root cause of the flash-overlay stuck-opaque bug (`lib/features/flash/flash_overlay.dart`).
