# Code-reviewer memory index

- [Stopwatch PR patterns](project_stopwatch_pr_patterns.md) — mutation-queue/.then()/multi-line-comment/GestureDetector-300ms/cross-controller-ref.read patterns; mutation-queue skeleton now 4 instances (2026-08-09, flash-points PR) — extraction is now a Warning, not Suggestion. `_applyStartupRules` (flash_points_controller.dart) was flagged for length 2026-08-09 (commit 3c569c4) and fixed same-PR (commit 4c8ddfc) by extracting `_defaultsToKeep`/`_customsToKeep`
- [README maintenance gap](project_readme_maintenance_gap.md) — recurred 7x through 2026-08-09; user accepted the fix, global CLAUDE.md workflow now has an explicit README-check step; stop escalating unless it recurs post-fix (a compliance issue, not a missing rule)
- [Notification scheduler patterns](project_notification_scheduler_patterns.md) — race/init-memoization fixed 2026-08-09; init()-retry untested; #39: requestExactAlarmsPermission() verified no re-prompt-once-granted; priority-field test gap resolved same PR; channel id bumped to flash_points_v2 (Android channels are immutable once created)
- [Flash-point toggle patterns](project_flash_point_toggle_patterns.md) — invariant-in-constructor, >20-line-build()/SettingsListItem-dupe, admission-only-queue-purge, and purge-trusting-AsyncError-emptied-.value all fixed same-PR (2026-08-09, through commit after ebf31f8)
- [NTP sync patterns](project_ntp_sync_patterns.md) — main()-only network-isolation verified safe; issue #36 binding-race fix verified; issue #42 IPv4-fallback (`preferIPv4Address`) verified correct, `@visibleForTesting` is a good precedent; doc-comment issue-# self-reference recurred 4th time; issue #59 (2026-08-16): missing `INTERNET` permission in release-only manifest fixed correctly, doc-comment self-reference rule stayed clean this time, but "auto-grants" wording is imprecise (it's manifest-merge from sibling src/debug+src/profile files, not a runtime grant)
- [Session-schedule patterns](project_session_schedule_patterns.md) — DataTable vertical-overflow bug fixed+verified in PR #45 follow-up; `_nearestPast`/`isToday` ongoing-WE fix verified correct by hand-trace; doc-comment plan-file/caller self-reference recurred 6th+ time in issue #48 screen-split PR (a741e85); `_isVisibleOnScheduleScreen` visibility filter verified correct (keyed off sequence number, gap calc unaffected); `_singletonTypes` dup is pre-existing, relocated not new; `session_event_test.dart` now exists (confirmed present since a10c206) with good `tryFromJson`/`toJson` coverage incl. `visible` — prior "missing dedicated test file" note was stale, corrected; issue #50 manualNumber (7fadaf7): display-only invariant verified holding; DRY dup + `_EventRow.build` length + doc-comment self-reference all RESOLVED same PR (a30ed5f); CodeRabbit follow-up: `editNumber_<id>` GestureDetector→InkWell for keyboard focus/activation, tested; issue #52 (2026-08-14): `GlobalMaterialLocalizations.delegates` verified real against installed SDK; `intl` 0.20.2 pin is SDK-forced not a mistake; new DateTime(2000)/(2100) dup + `_AddEventFormState.build` ~40-line regrowth + for-loop-vs-firstWhere all first-time findings, not recurring; PR #53 fix-up (2026-08-15): `intl: ^0.20.2` CI fix verified sound, `_clampToScheduleRange` resolves the prior DRY note, strengthened widget test verified correct by hand-trace — also: never mutate source to test-the-test, classifier blocks it and it breaks the reviewer's read-only role
- [Flash-event builder patterns](project_flash_event_builder_patterns.md) — issue #46: `completionFlashEvents`/`timerFlashEvents` structural dupe RESOLVED same PR via shared `_exactPlusMinutesBefore`; id/queue/notification wiring and test coverage all verified clean
- [Home-widget Android patterns](project_home_widget_android_patterns.md) — issue #54: Critical — `StopwatchWidgetProvider.kt` hardcodes Chronometer `started=true`, ignores pause; 3-provider DRY dupe; for-loop-vs-firstWhere 2nd instance; issue-# comment in AndroidManifest.xml (1st XML instance); home_widget v0.9.3 plugin API + Int32/Int64 rationale verified against real plugin source; issue #61 (2026-08-17): resource/manifest-only picker-label/cell-size unify, all receiver↔xml↔string mappings verified correct, minHeight=90dp kept deliberately (not a regression), clean PR; issue #62/PR #67 (2026-08-17): interactive widget buttons — reloadFromDisk() try/catch and buildViews length both fixed same PR (before merge, in response to code-reviewer's own first pass); issue-#-self-reference now also in Kotlin doc comments + 2nd XML instance, both removed; CodeRabbit follow-up caught a real accessibility gap (toggle/reset buttons under 48dp touch target) and a test-fidelity gap (fake gateway's `valueOf` threw instead of returning null); new "(see other_file.dart)" caller-pointer comment sub-pattern judged OK — matches this repo's own pre-existing "See TimeTargetsController (path) for why..." convention, not a caller/task self-reference; post-CI-green on-device (BlueStacks) verification found CodeRabbit's own 150dp width "fix" was STILL wrong — buttons were entirely missing from the inflated view tree (not just clipped), invisible to any build/analyze/review check, only found by actually placing the widget and dumping the real hierarchy; fixed by widening to 220dp — see [[project_home_widget_android_patterns]] for the full story and a second (unfixed, documented-as-known-limitation) `home_widget` plugin WorkManager bug found the same way; issue #70 (2026-08-17): operable timer widget reusing #62's button pattern — 4 issue-#-self-references in Dart/Kotlin doc comments, byte-identical `applyChronometerOrPlaceholder`+base-calc duplicated between `TimerControlWidgetProvider.kt`/`TimerWidgetSync.kt`, and `_FakeHomeWidgetChannel` test fake copy-pasted into 3 test files were all RESOLVED same PR (first time this DRY class has actually been fixed rather than deferred, since #54) — extracted `TimerWidgetCountdown.kt` and `test/features/home_widget/fake_home_widget_channel.dart`; 220dp width lesson and no-XML-comment lesson from #62 both applied proactively this time; issue #64 (2026-08-17): first `RemoteViewsService`/`RemoteViewsFactory`-backed widget (scrollable schedule `ListView`) — zero comment-policy violations (first clean pass in this area since #54); JSON try/catch, missing day-only-recompute test, and fixed-width label-clip risk all RESOLVED same PR (incl. on-device BlueStacks verification, `input draganddrop` needed since a bare long-press+swipe didn't register as a widget-picker drag); `setRemoteAdapter(viewId, Intent)` 2-arg (non-deprecated) vs 3-arg (deprecated) overload verified correct; issue #74 (2026-08-17): `applyGapRow`'s 3-call visibility logic verified correct (fresh RemoteViews per row + unconditional visibility calls rules out stale-text-while-GONE recycling bug), zero comment-policy violations, plan-file 180dp/220dp-incident rationale verified accurate; follow-up same day: Gemini CLI caught a real marginStart-relative-to-cursor indent bug missed here, its own `<View>` spacer fix crashed RemoteViews inflation (plain View not on RemoteViews' widget whitelist) and was corrected to a textless TextView spacer, verified on-device — 2nd confirmed "AI fix syntactically fine, wrong RemoteViews-specific constraint, only caught on-device" instance in this feature area (1st was #62's 150dp width)
- [Timer widget display patterns](project_timer_widget_display_patterns.md) — issue #63/PR #69: Critical `TimerState.targetEpochMs` raw-device-clock vs NTP-corrected NextTarget/Completion, plan-file citation, and missing scheduler-level test all RESOLVED before merge (fixed same-PR in response to code-reviewer's own first pass); CodeRabbit follow-up: flash-window boundary made half-open (`>= start && < end`) — legitimate; CodeRabbit's suggestion to flip the window to `[instant, instant+3s)` was a false positive, verified wrong against `flash_event.dart`'s explicit `FlashEvent.windowStart` contract ("animation must *end* at instant") and rejected
- [Android release signing patterns](project_android_release_signing_patterns.md) — issue #76 (2026-08-18): first Gradle Kotlin DSL review; fallback/gitignore logic verified sound; unclosed `InputStream` and unchecked `as String` Properties casts both flagged and fixed in-PR; debug-signing fallback for missing key.properties kept intentionally (matches Flutter's own recommended pattern); likely more Gradle/CI PRs coming in this Play Store rollout area
- [Session plan patterns](project_session_plan_patterns.md) — issue #78 (2026-09-03): 3-stage dialog cancel-flow verified correct (no #79-style clobber bug); `upsertTarget` consistent+tested; mutation-queue skeleton now 6 instances (still unresolved, see [[project_stopwatch_pr_patterns]]); doc-comment issue-#/caller self-reference recurred ~7x in one PR incl. a new "names the caller UI button text" sub-variant; entry-button widget dup is 2nd instance (Suggestion, not yet Warning)
