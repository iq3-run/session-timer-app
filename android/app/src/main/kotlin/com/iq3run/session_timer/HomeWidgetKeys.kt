package com.iq3run.session_timer

/** Must match the key strings in lib/features/home_widget/home_widget_sync_service.dart. */
object HomeWidgetKeys {
    const val NTP_OFFSET_MS = "ntp_offset_ms"
    const val STOPWATCH_ACCUMULATED_MS = "stopwatch_accumulated_ms"
    const val STOPWATCH_RUNNING_SINCE_EPOCH_MS = "stopwatch_running_since_epoch_ms"
    const val NEXT_TARGET_EPOCH_MS = "next_target_epoch_ms"
    const val COMPLETION_TARGET_EPOCH_MS = "completion_target_epoch_ms"
}
